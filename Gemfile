# frozen_string_literal: true

source "https://rubygems.org"
gemspec

# For GitHub Pages compatibility, you can also use:
# gem "github-pages", group: :jekyll_plugins
# But this may override theme dependencies